import Foundation

let str = readLine()!

if str == "M" { print("MatKor") }
else if str == "W" { print("WiCys") }
else if str == "C" { print("CyKor") }
else if str == "A" { print("AlKor") }
else { print("$clear") }
